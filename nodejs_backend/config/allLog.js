const config = {
    logConfig: {
        logFolder: './complete_logs/',  // Folder where log files will be saved
        logFile: '%DATE%.log',          // File naming pattern using placeholder
    }
};

export default config;  // Export the config object for use in other modules